import React from 'react'
import Layout from '../components/Layout'
const Invitations = ({location}) => {
    return (
        <Layout>
            <h1>Welcome to the Invitations Page</h1>
        </Layout>
    )
}

export default Invitations;